spring:
#  datasource:
#    url: jdbc:h2:mem:test  #외부 디비가 아닌 메모리 모드로 동작.
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create     # 애플리케이션 실행 시점에 내가 가지고 있는 테이블을 다 지우고 재 생성.
#    properties:
#      hibernate:
#        #        show_sql: true     #system.out.print로 출력하는 것
#        format_sql: true
# 스프링부트는 기본적으로 메모리 모드로 동작하기때문에 별도의 설정 없어도 메모리에 저장된다.
logging:
  level:
    org.hibernate.SQL: debug                #log출력.
    org.hibernate.type: trace